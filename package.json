{
  "name": "todomvc-lit",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.13.0",
    "npm": ">=8.19.3"
  },
  "scripts": {
    "dev": "wds",
    "build": "tsc && mkdir -p dist && cp index.html dist/ && cp node_modules/todomvc-common/base.{js,css} dist/ && cp -r lib/* dist/",
    "tsc": "tsc"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "@web/dev-server-esbuild": "^1.0.4",
    "todomvc-common": "^1.0.5",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "lit": "^3.0.0-pre.0",
    "tslib": "^2.5.2"
  }
}
